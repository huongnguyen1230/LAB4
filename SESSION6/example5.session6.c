#include <stdio.h>
#include <stdlib.h>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */

int main(int argc, char *argv[]) {
	int a = 80;
	char b = 'c';
	printf ("this is the number stored in 'a' %d\n", a);
	printf ("this is a character interpreted from 'a' %c\n", a);
	printf ("this is also a character stored in 'b' %c\n",b);
	printf ("hey! the character of 'b'is printed as a number! %d", b);
	return 0;
}
